{
  "name": "haufe-server",
  "version": "1.0.0",
  "description": "api rest to provide list of objects and object by id",
  "main": "server.js",
  "scripts": {
    "test-watch": "NODE_ENV=test nodemon --exec 'mocha --recursive -R min'",
    "test": "mocha --recursive"
  },
  "author": "danaya",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.16.0",
    "express": "^4.14.1",
    "mocha": "^3.2.0",
    "nodemon": "^1.11.0",
    "supertest": "^3.0.0"
  }
}
